fn main() {
    // let idx = StaticMortonIndex3D32::try_from([Octant::One].as_slice()).unwrap();
    // let grid_index = idx.to_grid_index(OctantOrdering::XYZ);
    // let roundtrip =
    //     StaticMortonIndex3D32::from_grid_index(grid_index, 1, OctantOrdering::XYZ).unwrap();
}
